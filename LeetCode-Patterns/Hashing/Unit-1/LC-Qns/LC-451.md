```java
Past Debacle
class Solution {

    /*static class Pair{

        char alph;

        int freq;

  

        Pair(){

            alph = '\0';

            freq = -1;

        }

  

        Pair(char alph, int freq){

            this.alph = alph;

            this.freq = freq;

        }

    }*/

    //I rejected Pair cus i can't have O(1) lookup while counting freq

    public String frequencySort(String s) {

        //Pair obj = new Pair(); Test syntax

        //THis is just normal counting sort

        //Idk why i got stuck

        //Why i overthinking and shi

        //Just apply the freaking algorithm and thts it

        //Once u sort the Array of Pairs then iterate from back to reconstruct

        //EZ af idk why i struggled for 2 hrs

  

        //Idk how to sort if they add constraint like alphabetic if equal bruh im cooked

        //Turn string into Pair array

        int n = s.length();

        //Pair[] arr = new Pair[n]; //Worst case atmost n possible

        Map<Character,Integer> freqMap = new HashMap<>();

  

        for(int i=0; i<n; i++){

            freqMap.put(s.charAt(i), freqMap.getOrDefault(s.charAt(i), 0)+1);

            //Increment the count;

        }

        int max = -1;

        for(int v : freqMap.values()){

            //System.out.println(entry.getValue());

            max = Math.max(max,v);

        }

  

        //System.out.println(max);

        //Make index Arr

        int[] index = new int[max+1];

  

        for(int v : freqMap.values()){

            //System.out.println(v);

            index[v]++;

        }

        /*for(int v : index){

            System.out.println(v);

        }*/

  

        //Cumulative sum

        for(int i=max-1; i>0; i--){

            index[i] = index[i] + index[i+1];

            //System.out.println(index[i]);

        }

  

        for(int v : index){

            System.out.println(v);

        }

        //I did in reverse cus wat i want to achieve is the opposite

        //im still confused about the entire proceedure

        //I dont get wat im doing !!!!! Somehow it works

  
  

  

        //Boilerplate ready now apply it

        char[] buf = new char[n];

        //Get index from index

        //Get loop count from pair.freq

  

        //Iterate from back for some reason

        //Literally im cargoculting i hate it

        for(char c : freqMap.keySet()){

            int k = freqMap.get(c);

            System.out.println(c + " : " +  index[k]);

            //Maybe place then decrement?

            //Usually we decrement then place idk its so confusing

            buf[index[k]] = c;

            index[k] = index[k]-1;

        }

  

        System.out.println(new String(buf));

  
  

        return new String(); //Don't interfere

    }

}
```